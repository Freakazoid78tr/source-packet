# Environment variables required by this Hermes profile distribution.
# Copy to .env and fill in your own values if using the default config.

# Optional if you edit config.yaml to use another Hermes-supported provider.
OPENROUTER_API_KEY=
